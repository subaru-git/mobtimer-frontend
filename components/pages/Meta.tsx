import React, { FC } from "react";
import Head from "next/head";

const Meta: FC = () => {
  return (
    <Head>
      <meta name="robots" content="noindex" />
    </Head>
  );
};

export default Meta;
